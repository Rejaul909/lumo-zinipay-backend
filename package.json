{
  "name": "lumo-zinipay-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend for LUMO landing page — creates and verifies ZiniPay bKash/Nagad advance payments without ever exposing the API key to the browser.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
